# kdelta runs with its own identity and NO permissions by default — each
# overlay grants exactly the read access its cluster needs (see the demo
# overlay's rbac.yaml). In dev (kdelta run outside the cluster) the user's
# kubeconfig context applies instead.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kdelta
  labels:
    app.kubernetes.io/name: kdelta
